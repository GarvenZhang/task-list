export const mutationTypes = {}
export const actionTypes = {}

const initialState = {}
const getters = {}
const mutations = {}
const actions = {}

export default {
  state: initialState,
  getters,
  mutations,
  actions
}
